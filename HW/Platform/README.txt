

The Platform project is used to abstract away the hardware dependent code.

It has a common interface (API) defined in "inc/platform.h". 
For each supported platform, this interface is implemented in "src/platform_name".


